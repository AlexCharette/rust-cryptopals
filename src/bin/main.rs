use cryptopals::convert::*;
use cryptopals::decrypt::*;

fn main() {
    // Run ex 2.2

    // TODO read file content
    
    base64_to_hex(b64_str);
    // TODO convert file content to bytes
    
    dec_xor_cbc(bytes, key)
}
